# apk-ui-parse-sample

Android 宿主示例工程。

这是验证工程，不是主产品。

用于放置：

- 权限引导页面
- 导出触发页面
- JSON 展示或文件保存
- 真机联调用例

开源仓库角色：

- 演示如何接入 `apk-ui-parse-core` 和 `apk-ui-parse-android`
- 用于真机测试和回归验证
- 不作为最终交付物

建议包前缀：

- `com.apkparse.sample`
